# Program to find the frequency of an element

numbers = [10, 20, 30, 20, 40, 20, 50]

key = int(input("Enter the element to find frequency: "))

count = 0
for num in numbers:
    if num == key:
        count += 1

print(f"Frequency of {key} is {count}")
